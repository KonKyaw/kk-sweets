export interface AppProduct {
    key?: string,
    title: string,
    price?: number,
    category: string,
    imageUrl?: string,
    downloadUrl?: string,
    note?: string,
    createdDate?: Date,
    updatedDate?: Date,
    createdUser?: string,
    updatedUser?: string,
}